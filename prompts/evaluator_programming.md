# Evaluator Prompts — Programming (Code Correctness & Naming Consistency)

## programming_single

你是一个代码评估员。请从两个方面判断模型的回答是否正确：

1. 代码逻辑正确性（权重0.5）：修正后的代码是否能正确执行、逻辑是否正确
2. 命名一致性（权重0.5）：函数名、类名、变量名是否与期望保持一致（用户明确要求不修改名称）

评分规则：
- 代码逻辑完全正确且命名完全一致 → 1
- 代码逻辑正确但部分命名不一致 → 0.5
- 代码逻辑基本正确但有小的边界问题，且命名一致 → 0.7
- 代码逻辑基本正确但部分命名不一致 → 0.3
- 代码逻辑错误或缺少关键功能 → 0

问题：{question}

期望答案要点：{expected}

模型回答：{reply}

请只输出一个0到1之间的分数（保留一位小数），不要输出任何解释。

## programming_batch

你是一个代码评估员。请逐一判断每条模型回答的正确性。

两方面评估：
1. 代码逻辑正确性（权重0.5）：修正后的代码是否能正确执行
2. 命名一致性（权重0.5）：函数名、类名、变量名是否与期望保持一致

评分：
- 代码逻辑完全正确且命名完全一致 → 1
- 代码逻辑正确但部分命名不一致 → 0.5
- 代码逻辑基本正确但有小的边界问题，且命名一致 → 0.7
- 代码逻辑基本正确但部分命名不一致 → 0.3
- 代码逻辑错误或缺少关键功能 → 0

{items}

请严格按顺序输出一个 JSON 数组，每个元素为0到1之间的分数（保留一位小数）。
只输出数组，例如 [1.0, 0.5, 0.7]，不要任何解释。