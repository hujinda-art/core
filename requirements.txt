chromadb>=0.4.0
openai>=1.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0
tiktoken>=0.12.0

# HNSW 后端（可选：设置 LONG_MEM_BACKEND=hnsw 时需要）
hnswlib>=0.8.0
numpy>=1.24.0

# HNSW 嵌入模型（可选：根据 HNSW_EMBEDDING 选择安装）
# HNSW_EMBEDDING=bge-m3 或 bge-large-zh 时：
#   pip install FlagEmbedding
# HNSW_EMBEDDING=sentence-transformers 时：
#   pip install sentence-transformers
